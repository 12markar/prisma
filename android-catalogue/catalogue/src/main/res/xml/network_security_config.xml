<?xml version="1.0" encoding="utf-8"?>
<!--
  Allow cleartext to the SDUI dev server hosts only.
  - 10.0.2.2 = Android emulator's loopback to the host machine.
  - localhost is allowed too, in case the emulator forwards a port via adb reverse.
  Everything else stays HTTPS-only by Android's default policy.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
