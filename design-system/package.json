{
  "name": "prisma-tokens",
  "version": "0.0.0",
  "private": true,
  "description": "Token build pipeline. Source of truth lives in tokens/. Style Dictionary generates Tokens.kt (Compose) and Tokens.swift (SwiftUI) into each catalogue app's source tree.",
  "scripts": {
    "build-tokens": "node ./scripts/build.mjs",
    "lint-tokens": "node ./scripts/lint.mjs",
    "check-contrast": "node ./scripts/check-contrast.mjs",
    "copy-fonts": "node ./scripts/copy-fonts.mjs",
    "build": "npm run build-tokens && npm run copy-fonts",
    "verify": "npm run lint-tokens && npm run check-contrast"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "style-dictionary": "^4.3.0"
  }
}
