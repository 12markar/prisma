// Components target — populated per build phase.
//
// Phase 1 lands Button. Subsequent phases add TextField, Checkbox, Radio, Switch,
// Slider, SegmentedControl, Toast, Banner, Modal, BottomSheet, Loading, Skeleton,
// Badge, Tabs, Chip, Card, ListItem, Avatar, Tooltip, Popover, EmptyState,
// CommandPalette and the foundation showcase views.

import Foundation

public enum PrismaComponents {
    public static let version = "0.0.0-phase0"
}
